models:
  Todo:
    model: github.com/swiftcarrot/gqlgen/example/todo.Todo
  ID:
    model: # override the default id marshaller to use ints
      - github.com/swiftcarrot/gqlgen/graphql.IntID
      - github.com/swiftcarrot/gqlgen/graphql.ID
